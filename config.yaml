app:
  name: KasirApp
  port: 8080

database:
  host: localhost
  port: 5432
  user: postgres
  password: ${DB_PASSWORD}
  name: kasir_db
  ssl_mode: disable
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: 1h
  conn_max_idle_time: 30m
