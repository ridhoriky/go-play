pre-commit:
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run --fix {staged_files}
      stage_fixed: true

pre-push:
  commands:
    lint-all:
      run: golangci-lint run
    test:
      run: go test ./...
