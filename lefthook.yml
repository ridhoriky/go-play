pre-commit:
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run --fix ./...
      stage_fixed: true

pre-push:
  commands:
    lint-all:
      run: golangci-lint run
    test:
      run: go test ./...
